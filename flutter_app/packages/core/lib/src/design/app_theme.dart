import 'package:flutter/material.dart';

/// Theme shell. Tokens and components come later.
class AppTheme {
  const AppTheme._();

  static ThemeData light() => ThemeData.light();

  static ThemeData dark() => ThemeData.dark();
}
